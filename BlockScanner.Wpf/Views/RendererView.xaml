<UserControl x:Class="BlockScanner.Wpf.Views.RendererView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BlockScanner.Wpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="Pink">
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="20"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock HorizontalAlignment="Left" Grid.Row="0">
            <TextBlock.Text>
                <MultiBinding StringFormat="{}Renderer {0}">
                    <Binding Path="Renderer.Renderer" />
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>
        <TextBlock HorizontalAlignment="Left" Grid.Row="1">
            <TextBlock.Text>
                <MultiBinding StringFormat="{}Sample region: {0}x{1} @ ({2}, {3})">
                    <Binding Path="ScanArea.Width" />
                    <Binding Path="ScanArea.Height" />
                    <Binding Path="ScanArea.X" />
                    <Binding Path="ScanArea.Y" />
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>
        <Border BorderBrush="Gray" BorderThickness="1"  Grid.Row="2">
            <Image x:Name="CapturePreview" Source="{Binding Path=CapturePreview}" />
        </Border>
    </Grid>
</UserControl>
