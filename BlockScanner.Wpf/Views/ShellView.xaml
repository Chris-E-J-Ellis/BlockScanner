<Window x:Class="BlockScanner.Wpf.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="400" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <UniformGrid Columns="2" Grid.Row="0">
            <Button x:Name="BeginSelection" Content="Select Capture Area" />
            <Button x:Name="DumpScanArea" Content="Dump Scan Area (./Images/cap.bmp)" />
            <Button x:Name="CreateScanner" Content="Create Scanner" />
            <Button x:Name="ToggleConsole" Content="Toggle Console" />
            <ComboBox ItemsSource="{Binding Path=Detectors}" SelectedItem="{Binding Path=SelectedDetector}" />
            <ComboBox ItemsSource="{Binding Path=ValidRenderers}" SelectedItem="{Binding Path=SelectedRenderer}" />
            <Button x:Name="RunTestArea" Content="Run Test (Area x = 0, y = 0 , 400x400)" />
        </UniformGrid>
        <ContentControl x:Name="Scanner" Grid.Row="1"/>
    </Grid>
</Window>